# 📘 База данных проекта «Кухни Вязники»

## Общая информация

База данных построена на **PostgreSQL** и используется для хранения данных интернет-сайта мебельного магазина.  
Основная цель структуры — обеспечить масштабируемость, чистоту данных и удобство интеграции с **FastAPI + SQLAlchemy**.

## Составные части

- **Пользователи и авторизация**
- **Категории и товары**
- **Характеристики товаров**
- **Отзывы**
- **Реализованные проекты**
- **Записи на замер**
- **Рекламные баннеры**

---

## 1. Таблица `users` — пользователи

| Поле          | Тип                                  | Назначение                            |
| ------------- | ------------------------------------ | ------------------------------------- |
| id            | serial (PK)                          | Уникальный идентификатор пользователя |
| full_name     | text                                 | Полное имя                            |
| phone         | text                                 | Телефон                               |
| email         | text (unique)                        | Электронная почта                     |
| password_hash | text                                 | Захешированный пароль                 |
| role          | enum(`admin`, `manager`, `customer`) | Роль пользователя                     |
| created_at    | timestamp                            | Дата создания                         |
| updated_at    | timestamp                            | Дата обновления                       |

---

## 2. Таблица `categories` — категории товаров

| Поле       | Тип                          | Назначение             |
| ---------- | ---------------------------- | ---------------------- |
| id         | serial (PK)                  | ID категории           |
| name       | text                         | Название               |
| slug       | text                         | URL-идентификатор      |
| parent_id  | int (FK → categories.id)     | Родительская категория |
| type       | enum(`kitchen`, `furniture`) | Тип категории          |
| created_at | timestamp                    | Дата создания          |

---

## 3. Таблица `products` — товары (кухни и мебель)

| Поле        | Тип                          | Назначение      |
| ----------- | ---------------------------- | --------------- |
| id          | serial (PK)                  | ID товара       |
| category_id | int (FK → categories.id)     | Категория       |
| name        | text                         | Название        |
| slug        | text                         | SEO-slug        |
| description | text                         | Описание        |
| price       | numeric                      | Цена            |
| is_new      | bool                         | Новинка         |
| is_hit      | bool                         | Хит продаж      |
| type        | enum(`kitchen`, `furniture`) | Тип             |
| created_at  | timestamp                    | Дата создания   |
| updated_at  | timestamp                    | Дата обновления |

---

## 4. Таблица `product_images` — изображения товаров

| Поле       | Тип                    | Назначение            |
| ---------- | ---------------------- | --------------------- |
| id         | serial (PK)            | ID изображения        |
| product_id | int (FK → products.id) | Товар                 |
| image_url  | text                   | Ссылка на изображение |
| is_main    | bool                   | Главное изображение   |

---

## 5. Таблица `attributes` — характеристики

| Поле | Тип             | Назначение              |
| ---- | --------------- | ----------------------- |
| id   | serial (PK)     | ID характеристики       |
| name | text            | Название характеристики |
| unit | text (nullable) | Единица измерения       |

---

## 6. Таблица `product_attributes` — значения характеристик

| Поле         | Тип                      | Назначение        |
| ------------ | ------------------------ | ----------------- |
| product_id   | int (FK → products.id)   | ID товара         |
| attribute_id | int (FK → attributes.id) | ID характеристики |
| value        | text                     | Значение          |

*(многие ко многим через product_id + attribute_id)*

---

## 7. Таблица `reviews` — отзывы

| Поле        | Тип                              | Назначение             |
| ----------- | -------------------------------- | ---------------------- |
| id          | serial (PK)                      | ID отзыва              |
| product_id  | int (nullable, FK → products.id) | Отзыв к товару         |
| user_id     | int (nullable, FK → users.id)    | Автор                  |
| author_name | text                             | Имя автора             |
| rating      | int                              | Оценка (1–5)           |
| text        | text                             | Текст отзыва           |
| created_at  | timestamp                        | Дата публикации        |
| is_approved | bool                             | Одобрен ли модератором |

---

## 8. Таблица `projects` — реализованные проекты

| Поле        | Тип         | Назначение      |
| ----------- | ----------- | --------------- |
| id          | serial (PK) | ID проекта      |
| name        | text        | Название        |
| description | text        | Описание        |
| location    | text        | Адрес или район |
| created_at  | timestamp   | Дата добавления |

---

## 9. Таблица `project_images` — фото реализованных проектов

| Поле       | Тип                    | Назначение            |
| ---------- | ---------------------- | --------------------- |
| id         | serial (PK)            | ID изображения        |
| project_id | int (FK → projects.id) | Проект                |
| image_url  | text                   | Ссылка на изображение |
| is_main    | bool                   | Главное фото          |

---

## 10. Таблица `project_products` — связь проектов и товаров

> Эта таблица реализует связь "многие ко многим" между `projects` и `products`.  
> Она позволяет хранить список товаров, использованных в каждом проекте.

| Поле       | Тип                    | Назначение           |
| ---------- | ---------------------- | -------------------- |
| project_id | int (FK → projects.id) | Проект               |
| product_id | int (FK → products.id) | Использованный товар |

*(массив ссылок на товары реализуется через выборку по project_id)*

---

## 11. Таблица `measure_requests` — заявки на замер

| Поле           | Тип                                             | Назначение            |
| -------------- | ----------------------------------------------- | --------------------- |
| id             | serial (PK)                                     | ID заявки             |
| full_name      | text                                            | Имя клиента           |
| phone          | text                                            | Телефон               |
| address        | text                                            | Адрес                 |
| preferred_date | date (nullable)                                 | Предпочтительная дата |
| comment        | text (nullable)                                 | Комментарий           |
| status         | enum(`new`, `in_progress`, `done`, `cancelled`) | Статус заявки         |
| created_at     | timestamp                                       | Дата создания         |

---

## 12. Таблица `banners` — рекламные баннеры

| Поле      | Тип             | Назначение        |
| --------- | --------------- | ----------------- |
| id        | serial (PK)     | ID баннера        |
| title     | text            | Заголовок         |
| image_url | text            | Изображение       |
| link_url  | text (nullable) | Ссылка            |
| position  | int             | Порядок вывода    |
| is_active | bool            | Активен ли баннер |

---

## 🔗 Основные связи между таблицами

- **categories → products** — 1 ко многим  
- **products → product_images** — 1 ко многим  
- **products ↔ attributes** — многие ко многим через `product_attributes`  
- **products → reviews** — 1 ко многим  
- **projects → project_images** — 1 ко многим  
- **projects ↔ products** — многие ко многим через `project_products`

---

## 📌 Примечания

- Все даты (`created_at`, `updated_at`) рекомендуется заполнять автоматически на уровне ORM.  
- Для изображений предполагается использование CDN или S3-совместимого хранилища.  
- В будущем можно добавить таблицу `orders`, если появится онлайн-заказ.  
- Для админки можно использовать FastAPI Admin или кастомный фронт на Flutter Web.

---

**Автор:** Buddy & ChatGPT  
**Дата:** 2025-11-08  
**Версия:** 1.0
